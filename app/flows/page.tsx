"use client";
import GenericPage from "@/components/generic-page";
export default function FlowsPage() {
  return <GenericPage title="Flow Builder" description="JSONFlow editor, visual graph, node registry, validation, run/export controls." />;
}
