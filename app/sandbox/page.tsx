"use client";
import GenericPage from "@/components/generic-page";
export default function SandboxPage() {
  return <GenericPage title="Sandbox" description="Sandbox sessions, policy preview, terminal output, captured artifacts." />;
}
